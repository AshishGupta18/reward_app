// lib/presentation/widgets/scratch_card.dart

import 'package:flutter/material.dart';

class ScratchCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(
      child: Column(
        children: [
          Text('Scratch Card'),
          // Additional UI elements for scratch card
        ],
      ),
    );
  }
}